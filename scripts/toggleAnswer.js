// Toggles the display attribute of
// an element to be displayed or not.
function toggleAnswer(element) {
    if (element.style.display == "") {
        element.style.display = "block";
    } else {
        element.style.display = "";
    }
}
